fruits = ["apple", "banana", "cherry"]
for x in fruits:#iterate through fruits set
  if x == "banana": #condition
    continue #skip this iteration meanning that next code in block will be skipped and go next object
  print(x)