x = 15
y = 20
max_value = x if x > y else y # we can write them in one line and write code before the statement using conditional expression 
# it is the same as:
#if x>y:
#   maxvalue = x
#else:
#   maxvalue =y
print("Maximum value:", max_value)